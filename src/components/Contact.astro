---
import { MessageCircleMore } from "@lucide/astro";
import { contacts, socials, whatsApp } from "../data/data";
import Icons from "./ui/Icons.astro";
---


<section
    id="contacto"
    class="py-24 sm:py-32 bg-white text-black-600 relative"
>

    <div class="max-w-5xl mx-auto px-6">
        <div class="text-center mb-16 flex flex-col items-center">
            <MessageCircleMore class="size-16 mb-6 text-primary-500" />
            <h2 class="title-secundary title-style text-black-900">
                Contáctanos
            </h2>
            <p class="text-lg text-black-500 max-w-2xl mx-auto mt-4 font-medium">
                ¿Tienes un proyecto en mente o alguna duda? Escríbenos y te
                responderemos lo antes posible.
            </p>
        </div>

        <div
            class="flex flex-col lg:flex-row items-center justify-center gap-6"
        >
            {
                contacts.map((contact) => (
                    <a
                        href={contact.url}
                        class="contact-card"
                        target="_blank"
                        title={contact.name}
                        aria-label={contact.name}
                    >

                        <div class="contact-icon">
                            <Icons name={contact.name} />
                        </div>
                        <p class="text-black-800 font-semibold">{contact.info}</p>
                    </a>
                ))
            }
        </div>

        <div
            class="flex items-center gap-6 justify-center mt-12 border-b border-black-100 pb-12"
        >
            {
                socials.map((social) => (
                    <a
                        href={social.url}
                        class="social-media"
                        target="_blank"
                        title={social.name}
                        aria-label={social.name}
                    >
                        <Icons name={social.name} />
                    </a>
                ))
            }
        </div>

        <div class="flex items-center justify-center mt-12">
            {
                whatsApp && (
                    <a 
                        href={whatsApp.url}
                        target="_blank"
                        title={whatsApp.name}
                        aria-label={whatsApp.name}
                        class="contact-whatsapp"
                    >
                        <div>
                            <Icons name="whatsapp" />
                        </div>
                        <p>Escribir por WhatsApp</p>
                    </a>
                )
            }
        </div>
    </div>
</section>