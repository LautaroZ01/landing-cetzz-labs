---
import { ChevronLeft, ChevronRight, ArrowRight } from "@lucide/astro";

type Project = {
    title: string;
    description: string;
    image: string;
    link: string;
};

const projects: Project[] = [
    {
        title: "Todo Tela",
        description:
            "Landing page para una tienda de telas con un diseño moderno y responsive.",
        image: "/projects/todo-tela.webp",
        link: "https://www.tododetsalta.com.ar/",
    },
    {
        title: "AniTrendy",
        description:
            "Tienda online de moda con catálogo dinámico y diseño orientado a conversión.",
        image: "/projects/anitrendy.png",
        link: "https://anitrendy.com.ar/",
    },
    {
        title: "SkyCargo",
        description:
            "Sitio web corporativo de logística y transporte de carga con interfaz profesional.",
        image: "/projects/skycargo.png",
        link: "https://skycargo.com.ar/",
    },
    {
        title: "Impacto Creativo",
        description:
            "Landing page moderna y vibrante para una agencia de marketing digital.",
        image: "/projects/impactocreativo.png",
        link: "https://impacto-creativo.vercel.app/",
    },
    {
        title: "El Sabor Auténtico",
        description:
            "Landing page donde cada plato cuenta una historia y cada momento es único.",
        image: "/projects/sabor-autentico.webp",
        link: "https://resto-react-landing-page.vercel.app/",
    },
    {
        title: "RestoBar",
        description:
            "Landing page para un restobar con un diseño moderno y responsive.",
        image: "/projects/RestoBar.webp",
        link: "https://resto-astro-landing-page.vercel.app/",
    },
    {
        title: "Soluciones de Transporte",
        description:
            "Landing page para cotizar servicios de transporte para personas, materiales y carga.",
        image: "/projects/transporte.webp",
        link: "https://lta-landing-page-transporte-salta.netlify.app/",
    },
];
---

<section
    id="portafolio"
    class="py-24 lg:py-32 bg-labs relative overflow-hidden"
>
    <div class="rocket-bg top-[-5%] right-[-5%] opacity-50 mix-blend-screen">
    </div>
    <div
        class="absolute top-[30%] left-[-10%] w-[800px] h-[800px] bg-purple-900/10 rounded-full blur-[150px] pointer-events-none z-0"
    >
    </div>

    <div class="max-w-[90rem] mx-auto px-4 sm:px-6 w-full relative z-10">
        <div class="mb-8 md:mb-12">
            <div class="w-full portfolio-info-container">
                <span
                    class="text-cyan-500 font-bold tracking-widest uppercase text-sm mb-4 block"
                    >Nuestros proyectos</span
                >
                <h3
                    id="portfolio-title"
                    class="text-3xl md:text-5xl lg:text-5xl font-extrabold text-white mb-4 tracking-tight drop-shadow-md"
                >
                    {projects[0].title}
                </h3>
                <p
                    id="portfolio-desc"
                    class="text-lg md:text-xl text-gray-400 leading-relaxed font-light max-w-4xl min-h-[60px] md:min-h-[84px]"
                >
                    {projects[0].description}
                </p>
            </div>
        </div>

        <div
            class="relative w-full h-[250px] sm:h-[350px] md:h-[450px] lg:h-[500px] flex items-center justify-center perspective-[1500px]"
        >
            <div
                class="absolute -top-5 right-0 md:right-4 z-40 flex items-center gap-3"
            >
                <button
                    id="btn-prev"
                    class="w-10 h-10 md:w-12 md:h-12 flex items-center justify-center rounded-xl bg-[#080d17]/80 border border-white/10 hover:bg-white/20 hover:border-cyan-500/50 text-white transition-all active:scale-95 shadow-lg backdrop-blur-md cursor-pointer"
                    aria-label="Proyecto anterior"
                >
                    <ChevronLeft
                        class="w-5 h-5 md:w-6 md:h-6 pointer-events-none"
                    />
                </button>
                <button
                    id="btn-next"
                    class="w-10 h-10 md:w-12 md:h-12 flex items-center justify-center rounded-xl bg-[#080d17]/80 border border-white/10 hover:bg-white/20 hover:border-cyan-500/50 text-white transition-all active:scale-95 shadow-lg backdrop-blur-md cursor-pointer"
                    aria-label="Siguiente proyecto"
                >
                    <ChevronRight
                        class="w-5 h-5 md:w-6 md:h-6 pointer-events-none"
                    />
                </button>
            </div>500px]">
            {
                projects.map((project, index) => (
                    <div
                        class="carousel-item absolute top-0 w-full max-w-[95%] md:max-w-[85%] lg:max-w-[80%] h-full rounded-[1.5rem] overflow-hidden border border-white/10 shadow-[0_30px_60px_rgba(0,0,0,0.6)] transition-all duration-700 ease-[cubic-bezier(0.25,1,0.5,1)] cursor-pointer"
                        data-index={index}
                    >
                        <img
                            src={project.image}
                            alt={project.title}
                            class="w-full h-full object-cover object-top"
                        />

                        <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent opacity-80 pointer-events-none transition-opacity duration-500" />

                        <a
                            href={project.link}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="carousel-btn absolute bottom-6 right-6 md:bottom-8 md:right-8 inline-flex items-center justify-center gap-2 px-4 md:px-6 py-1 md:py-2 hover:bg-cyan-500 text-white font-bold text-sm md:text-base rounded-full transition-all active:scale-95 backdrop-blur-xl opacity-0 pointer-events-none translate-y-4"
                        >
                            Ver página
                            <ArrowRight class="w-4 h-4 md:w-5 md:h-5 pointer-events-none" />
                        </a>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<script is:inline define:vars={{ projects }}>
    window.portfolioProjects = projects;
</script>

<script>
    import { gsap } from "gsap";

    document.addEventListener("DOMContentLoaded", () => {
        const projects = (window as any).portfolioProjects;
        if (!projects || !projects.length) return;

        let currentIndex = 0;
        const total = projects.length;

        const items = document.querySelectorAll(".carousel-item");

        const titleEl = document.getElementById("portfolio-title");
        const descEl = document.getElementById("portfolio-desc");

        const btnPrev = document.getElementById("btn-prev");
        const btnNext = document.getElementById("btn-next");

        let isAnimating = false;

        const updateCarousel = () => {
            if (titleEl && descEl) {
                gsap.to([titleEl, descEl], {
                    opacity: 0,
                    y: 10,
                    duration: 0.2,
                    stagger: 0.05,
                    ease: "power1.in",
                    onComplete: () => {
                        titleEl.textContent = projects[currentIndex].title;
                        descEl.textContent = projects[currentIndex].description;

                        gsap.to([titleEl, descEl], {
                            opacity: 1,
                            y: 0,
                            duration: 0.4,
                            stagger: 0.05,
                            ease: "power2.out",
                        });
                    },
                });
            }

            const isMobile = window.innerWidth < 768;
            const shiftAmount = isMobile ? "15%" : "25%";
            const scaleAmount = isMobile ? 0.9 : 0.85;

            items.forEach((item, index) => {
                const el = item as HTMLElement;
                const btn = el.querySelector(".carousel-btn") as HTMLElement;

                if (index === currentIndex) {
                    el.style.transform = `translateX(0) scale(1) translateZ(0)`;
                    el.style.opacity = `1`;
                    el.style.zIndex = `30`;
                    el.style.filter = `blur(0px) brightness(1)`;
                    el.style.pointerEvents = "auto";

                    if (btn) {
                        btn.style.opacity = "1";
                        btn.style.pointerEvents = "auto";
                        btn.style.transform = "translateY(0)";
                    }
                } else if (index === getPrevIndex()) {
                    el.style.transform = `translateX(-${shiftAmount}) scale(${scaleAmount}) translateZ(-50px)`;
                    el.style.opacity = `0.6`;
                    el.style.zIndex = `20`;
                    el.style.filter = `blur(2px) brightness(0.6)`;
                    el.style.pointerEvents = "auto";

                    if (btn) {
                        btn.style.opacity = "0";
                        btn.style.pointerEvents = "none";
                        btn.style.transform = "translateY(1rem)";
                    }
                } else if (index === getNextIndex()) {
                    el.style.transform = `translateX(${shiftAmount}) scale(${scaleAmount}) translateZ(-50px)`;
                    el.style.opacity = `0.6`;
                    el.style.zIndex = `20`;
                    el.style.filter = `blur(2px) brightness(0.6)`;
                    el.style.pointerEvents = "auto";

                    if (btn) {
                        btn.style.opacity = "0";
                        btn.style.pointerEvents = "none";
                        btn.style.transform = "translateY(1rem)";
                    }
                } else {
                    el.style.transform = `translateX(0) scale(0.6) translateZ(-100px)`;
                    el.style.opacity = `0`;
                    el.style.zIndex = `10`;
                    el.style.filter = `blur(10px) brightness(0)`;
                    el.style.pointerEvents = "none";

                    if (btn) {
                        btn.style.opacity = "0";
                        btn.style.pointerEvents = "none";
                    }
                }
            });
        };

        const getPrevIndex = () => (currentIndex - 1 + total) % total;
        const getNextIndex = () => (currentIndex + 1) % total;

        let autoplayTimer: ReturnType<typeof setInterval>;

        const resetAutoplay = () => {
            clearInterval(autoplayTimer);
            autoplayTimer = setInterval(() => {
                if (!isAnimating) {
                    isAnimating = true;
                    currentIndex = getNextIndex();
                    updateCarousel();
                    setTimeout(() => (isAnimating = false), 700);
                }
            }, 5000);
        };

        const goToPrev = () => {
            if (isAnimating) return;
            isAnimating = true;
            currentIndex = getPrevIndex();
            updateCarousel();
            resetAutoplay();
            setTimeout(() => (isAnimating = false), 700);
        };

        const goToNext = () => {
            if (isAnimating) return;
            isAnimating = true;
            currentIndex = getNextIndex();
            updateCarousel();
            resetAutoplay();
            setTimeout(() => (isAnimating = false), 700);
        };

        btnPrev?.addEventListener("click", goToPrev);
        btnNext?.addEventListener("click", goToNext);

        items.forEach((item, index) => {
            item.addEventListener("click", (e) => {
                const target = e.target as HTMLElement;
                const isBtn = target.closest(".carousel-btn");

                if (index !== currentIndex && !isBtn) {
                    e.preventDefault();
                    if (isAnimating) return;
                    isAnimating = true;
                    currentIndex = index;
                    updateCarousel();
                    resetAutoplay();
                    setTimeout(() => (isAnimating = false), 700);
                }
            });
        });

        window.addEventListener("resize", () => {
            updateCarousel();
        });

        updateCarousel();
        resetAutoplay();
    });
</script>
